# PriorityQueue-RAG // Multi-Threaded Sovereign Query Scheduler

A native heap scheduler built to load-balance incoming pipeline queries safely based on absolute subscription priorities.

## Test Validation
```bash
python concurrent_rag_app.py
